# AquaLux Pool Care

A standalone Vite site for AquaLux pool maintenance in Cape Town.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
npm run preview
```

The contact buttons link directly to WhatsApp and the phone dialer. The two site photographs are stored in `public/assets`.
